no_cache;
import "app";
-- there is a bug in the code that if u provide a custom entry point for a project it will do the below
-- maybe we should deprecate this 
-- yh we will deprecate this feature and flag actually 
-- we will give a predefined entry point for new project creation
-- we will keep this #set_entry_point_to_here
-- so user can customize the project to however they like actually
-- lets like make the language by default execute via the main function actually
-- but we wont let users override main function
-- it will have the same signature as below
(*
    function Main() : Int {
        return App.Start();
    }
*)

function Main() : Int {
    App.Program();
    return 0;
}
