-- Host interface/syscall descriptors.
-- Klang code cannot perform host syscalls directly yet, so this module returns
-- structured Result/Table values that describe requested operations.

namespace interface {
    --- Signature: function IsAvailable() : Bool
    --- Returns: Bool.
    --- Summary: Checks whether is available is true for the provided Klang value(s).
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function IsAvailable() : Bool {
        return False;
    }

    --- Signature: function Unsupported(message : String) : String
    --- Returns: String.
    --- Summary: Provides the stdlib unsupported operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Unsupported(message : String) : String {
        return "interface unsupported: " + message;
    }

    --- Signature: function Operation(name : String, args : List[String]) : Table
    --- Returns: Table.
    --- Summary: Provides the stdlib operation operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Operation(name : String, args : List[String]) : Table {
        return {"name": name, "args": args, "available": IsAvailable()};
    }

    --- Signature: function Access(path : String, mode : Int) : Result[Bool, Atom]
    --- Returns: Result[Bool, Atom].
    --- Summary: Provides the stdlib access operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Access(path : String, mode : Int) : Result[Bool, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Open(path : String, mode : Int, perm : Int = 0) : Result[Table, Atom]
    --- Returns: Result[Table, Atom].
    --- Summary: Creates or initializes open with the supplied Klang value(s).
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Open(path : String, mode : Int, perm : Int = 0) : Result[Table, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Close(fd : Int) : Result[Int, Atom]
    --- Returns: Result[Int, Atom].
    --- Summary: Provides the stdlib close operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Close(fd : Int) : Result[Int, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Read(fd : Int, count : Int) : Result[String, Atom]
    --- Returns: Result[String, Atom].
    --- Summary: Reads, locates, or decodes read from the provided Klang input(s).
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Read(fd : Int, count : Int) : Result[String, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Write(fd : Int, data : String) : Result[Int, Atom]
    --- Returns: Result[Int, Atom].
    --- Summary: Builds, writes, or formats write using the provided Klang input(s).
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Write(fd : Int, data : String) : Result[Int, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Getenv(key : String) : Option[String]
    --- Returns: Option[String].
    --- Summary: Reads, locates, or decodes getenv from the provided Klang input(s).
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Getenv(key : String) : Option[String] {
        return None();
    }

    --- Signature: function Setenv(key : String, value : String) : Result[Int, Atom]
    --- Returns: Result[Int, Atom].
    --- Summary: Builds, writes, or formats setenv using the provided Klang input(s).
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Setenv(key : String, value : String) : Result[Int, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Environ() : List[String]
    --- Returns: List[String].
    --- Summary: Provides the stdlib environ operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Environ() : List[String] {
        return [];
    }

    --- Signature: function Getpid() : Int
    --- Returns: Int.
    --- Summary: Reads, locates, or decodes getpid from the provided Klang input(s).
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Getpid() : Int {
        return 0;
    }

    --- Signature: function Exit(code : Int) : Result[Int, Atom]
    --- Returns: Result[Int, Atom].
    --- Summary: Provides the stdlib exit operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Exit(code : Int) : Result[Int, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Socket(domain : Int, typ : Int, proto : Int) : Result[Int, Atom]
    --- Returns: Result[Int, Atom].
    --- Summary: Provides the stdlib socket operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Socket(domain : Int, typ : Int, proto : Int) : Result[Int, Atom] {
        return Err(:interface_unavailable);
    }

    --- Signature: function Stat(path : String) : Result[Table, Atom]
    --- Returns: Result[Table, Atom].
    --- Summary: Provides the stdlib stat operation using current Klang data types and runtime helpers.
    --- Contract: Preserve this typed interface when upgrading to a runtime-backed implementation.
    function Stat(path : String) : Result[Table, Atom] {
        return Err(:interface_unavailable);
    }

    @deprecated("Use IsAvailable instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function IS_AVAILABLE() : Bool { return IsAvailable(); }

    @deprecated("Use Unsupported instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function UNSUPPORTED(message : String) : String { return Unsupported(message); }

    @deprecated("Use Operation instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function OPERATION(name : String, args : List[String]) : Table {
        return Operation(name, args);
    }

    function Request(name : String, args : List[String]) : Result[Table, Atom] {
        return REQUEST(name, args);
    }

    @deprecated("Use Request instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function REQUEST(name : String, args : List[String]) : Result[Table, Atom] {
        local Table operation = Operation(name, args);
        if operation.available {
            return Ok(operation);
        }
        return Err(:interface_unavailable);
    }

    @deprecated("Use Access instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function ACCESS(path : String, mode : Int) : Result[Bool, Atom] {
        return Access(path, mode);
    }

    @deprecated("Use Open instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function OPEN(path : String, mode : Int, perm : Int = 0) : Result[Table, Atom] {
        return Open(path, mode, perm);
    }

    @deprecated("Use Close instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function CLOSE(fd : Int) : Result[Int, Atom] { return Close(fd); }

    @deprecated("Use Read instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function READ(fd : Int, count : Int) : Result[String, Atom] {
        return Read(fd, count);
    }

    @deprecated("Use Write instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function WRITE(fd : Int, data : String) : Result[Int, Atom] {
        return Write(fd, data);
    }

    @deprecated("Use Getenv instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function GETENV(key : String) : Option[String] { return Getenv(key); }

    @deprecated("Use Setenv instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function SETENV(key : String, value : String) : Result[Int, Atom] {
        return Setenv(key, value);
    }

    @deprecated("Use Environ instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function ENVIRON() : List[String] { return Environ(); }

    @deprecated("Use Getpid instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function GETPID() : Int { return Getpid(); }

    @deprecated("Use Exit instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function EXIT(code : Int) : Result[Int, Atom] { return Exit(code); }

    @deprecated("Use Socket instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function SOCKET(domain : Int, typ : Int, proto : Int) : Result[Int, Atom] {
        return Socket(domain, typ, proto);
    }

    @deprecated("Use Stat instead. Public stdlib functions now use UpperCamelCase for readability; this legacy spelling is retained only for source compatibility.")
    function STAT(path : String) : Result[Table, Atom] { return Stat(path); }
}
