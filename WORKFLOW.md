- Make a Workflow to do like releases of the project at the end of the day for 
    - Make the github do draft releases for my review to be uploaded
    - MACOS, WINDOWS, LINUX
    - Make the releases contain executable binaries for respective os
    - Make the release have a name convention of nightly-<day>-<month>
    - Dont make this occur everytime, make it occur if it has a new commit if no new commits were made during the day building a release is unnecessary do this when its the end of the day at 23:00 time period at sri lankan time zone

- Make a Workflow to do like releases of the project at the 2nd day of a new month 
    - Make the github do draft releases for my review to be uploaded
    - MACOS, WINDOWS, LINUX
    - Make the releases contain executable binaries for respective os
    - Make the release have a name convention of dev-<month>
    - If the user deletes the draft release then don't make a new one until the next cycle

- Make each releases have patch notes and information of any new features in it
- Make sure releases are not made every time if there is no new commit