# metasystem stdlib example

This example shows how to describe a kLang workspace with the experimental `metasystem` standard library module.

Run it from the repository root:

```sh
go run . run stdlib-examples/metasystem
```
