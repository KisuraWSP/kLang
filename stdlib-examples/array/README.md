# array stdlib example

This example is a deep tour of the experimental `array` standard library module.
It covers the function-style `array.*` helpers and the `Array(...)` alias object.

Run it from the repository root:

```sh
go run . run stdlib-examples/array
```

